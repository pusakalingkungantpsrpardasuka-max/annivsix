# AI Agent Instructions

This repository is a small static anniversary website.

## Key facts

- Single-page site: `index.html` is the only meaningful source file.
- No build system, no package manager, and no tests.
- Styling is inline in `index.html` using CSS variables and simple animations.
- The page is written in Indonesian and English; preserve the sentiment and tone.

## Recommended behavior

- Make only minimal changes needed to satisfy a request.
- Keep the layout and pastel heart-themed design intact unless the user asks for a redesign.
- Avoid introducing frameworks, external build tools, or unnecessary dependencies.
- Use semantic HTML and accessible styling when improving the page.

## When editing

- Update text content in `index.html` directly.
- Keep visual styling consistent with the existing pastel heart/rose theme.
- Preserve the responsive page layout and existing animations.

## Notes

- `README.md` contains just a short title and should remain unchanged unless the user asks for repository metadata updates.
